############################################
# Garmin Grafana stack (.env.example)
# Copy to .env and fill in real values.
# Do NOT commit .env.
############################################

# --- InfluxDB (shared by garmin-fetch-data; use same vars for CLI / future MCP read client)
INFLUXDB_VERSION=1
INFLUXDB_HOST=influxdb
INFLUXDB_PORT=8086
INFLUXDB_USERNAME=influxdb_user
INFLUXDB_PASSWORD=influxdb_secret_password
INFLUXDB_DATABASE=GarminStats
INFLUXDB_ENDPOINT_IS_HTTP=true

# Optional: dedicated READ-only user for MCP / automation (create in Influx with GRANT READ …)
# MCP_INFLUX_USERNAME=mcp_reader
# MCP_INFLUX_PASSWORD=

# --- Grafana HTTP API (optional — for MCP dashboard tools or scripts)
# Create token in Grafana: Administration → Service accounts (or API keys)
GRAFANA_URL=http://grafana:3000
GRAFANA_API_TOKEN=

# --- Agent memory sidecar (SQLite, not Influx) — used by `garmin-mcp`
# Default: ./data/agent-memory/findings.sqlite (created automatically)
# GARMIN_AGENT_MEMORY_DIR=/path/to/dir
# GARMIN_AGENT_MEMORY_DB=/path/to/findings.sqlite

# --- MCP HTTP gateway (`garmin-mcp-http` or compose profile `mcp-public`)
# MCP_HTTP_HOST=127.0.0.1
# MCP_HTTP_PORT=8765
# MCP_STREAMABLE_HTTP_PATH=/mcp
# openssl rand -hex 32
# MCP_AUTH_TOKEN=
# MCP_PUBLIC_HOSTS=mcp.example.com
